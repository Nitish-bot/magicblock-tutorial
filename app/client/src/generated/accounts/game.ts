/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type Option,
  type OptionOrNullable,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getGameResultDecoder,
  getGameResultEncoder,
  getGameStateDecoder,
  getGameStateEncoder,
  type GameResult,
  type GameResultArgs,
  type GameState,
  type GameStateArgs,
} from "../types";

export const GAME_DISCRIMINATOR = new Uint8Array([
  27, 90, 166, 125, 74, 100, 121, 18,
]);

export function getGameDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(GAME_DISCRIMINATOR);
}

export type Game = {
  discriminator: ReadonlyUint8Array;
  gameId: bigint;
  player1: Address;
  player2: Option<Address>;
  state: GameState;
  result: GameResult;
};

export type GameArgs = {
  gameId: number | bigint;
  player1: Address;
  player2: OptionOrNullable<Address>;
  state: GameStateArgs;
  result: GameResultArgs;
};

/** Gets the encoder for {@link GameArgs} account data. */
export function getGameEncoder(): Encoder<GameArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["gameId", getU64Encoder()],
      ["player1", getAddressEncoder()],
      ["player2", getOptionEncoder(getAddressEncoder())],
      ["state", getGameStateEncoder()],
      ["result", getGameResultEncoder()],
    ]),
    (value) => ({ ...value, discriminator: GAME_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link Game} account data. */
export function getGameDecoder(): Decoder<Game> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["gameId", getU64Decoder()],
    ["player1", getAddressDecoder()],
    ["player2", getOptionDecoder(getAddressDecoder())],
    ["state", getGameStateDecoder()],
    ["result", getGameResultDecoder()],
  ]);
}

/** Gets the codec for {@link Game} account data. */
export function getGameCodec(): Codec<GameArgs, Game> {
  return combineCodec(getGameEncoder(), getGameDecoder());
}

export function decodeGame<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<Game, TAddress>;
export function decodeGame<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<Game, TAddress>;
export function decodeGame<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<Game, TAddress> | MaybeAccount<Game, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getGameDecoder(),
  );
}

export async function fetchGame<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Game, TAddress>> {
  const maybeAccount = await fetchMaybeGame(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeGame<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Game, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeGame(maybeAccount);
}

export async function fetchAllGame(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Game>[]> {
  const maybeAccounts = await fetchAllMaybeGame(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeGame(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Game>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeGame(maybeAccount));
}
