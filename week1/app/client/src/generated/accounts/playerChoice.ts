/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type Option,
  type OptionOrNullable,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getChoiceDecoder,
  getChoiceEncoder,
  type Choice,
  type ChoiceArgs,
} from "../types";

export const PLAYER_CHOICE_DISCRIMINATOR = new Uint8Array([
  116, 20, 210, 159, 85, 200, 132, 149,
]);

export function getPlayerChoiceDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PLAYER_CHOICE_DISCRIMINATOR,
  );
}

export type PlayerChoice = {
  discriminator: ReadonlyUint8Array;
  gameId: bigint;
  player: Address;
  choice: Option<Choice>;
};

export type PlayerChoiceArgs = {
  gameId: number | bigint;
  player: Address;
  choice: OptionOrNullable<ChoiceArgs>;
};

/** Gets the encoder for {@link PlayerChoiceArgs} account data. */
export function getPlayerChoiceEncoder(): Encoder<PlayerChoiceArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["gameId", getU64Encoder()],
      ["player", getAddressEncoder()],
      ["choice", getOptionEncoder(getChoiceEncoder())],
    ]),
    (value) => ({ ...value, discriminator: PLAYER_CHOICE_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link PlayerChoice} account data. */
export function getPlayerChoiceDecoder(): Decoder<PlayerChoice> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["gameId", getU64Decoder()],
    ["player", getAddressDecoder()],
    ["choice", getOptionDecoder(getChoiceDecoder())],
  ]);
}

/** Gets the codec for {@link PlayerChoice} account data. */
export function getPlayerChoiceCodec(): Codec<PlayerChoiceArgs, PlayerChoice> {
  return combineCodec(getPlayerChoiceEncoder(), getPlayerChoiceDecoder());
}

export function decodePlayerChoice<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<PlayerChoice, TAddress>;
export function decodePlayerChoice<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<PlayerChoice, TAddress>;
export function decodePlayerChoice<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<PlayerChoice, TAddress> | MaybeAccount<PlayerChoice, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getPlayerChoiceDecoder(),
  );
}

export async function fetchPlayerChoice<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<PlayerChoice, TAddress>> {
  const maybeAccount = await fetchMaybePlayerChoice(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybePlayerChoice<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<PlayerChoice, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodePlayerChoice(maybeAccount);
}

export async function fetchAllPlayerChoice(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<PlayerChoice>[]> {
  const maybeAccounts = await fetchAllMaybePlayerChoice(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybePlayerChoice(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<PlayerChoice>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodePlayerChoice(maybeAccount));
}
