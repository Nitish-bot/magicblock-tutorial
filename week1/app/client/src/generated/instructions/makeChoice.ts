/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { MAGICBLOCK_TUTORIAL_PROGRAM_ADDRESS } from "../programs";
import {
  getChoiceDecoder,
  getChoiceEncoder,
  type Choice,
  type ChoiceArgs,
} from "../types";

export const MAKE_CHOICE_DISCRIMINATOR = new Uint8Array([
  207, 18, 251, 32, 135, 122, 160, 77,
]);

export function getMakeChoiceDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(MAKE_CHOICE_DISCRIMINATOR);
}

export type MakeChoiceInstruction<
  TProgram extends string = typeof MAGICBLOCK_TUTORIAL_PROGRAM_ADDRESS,
  TAccountPlayer extends string | AccountMeta<string> = string,
  TAccountGame extends string | AccountMeta<string> = string,
  TAccountPlayerChoice extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPlayer extends string
        ? WritableSignerAccount<TAccountPlayer> &
            AccountSignerMeta<TAccountPlayer>
        : TAccountPlayer,
      TAccountGame extends string
        ? WritableAccount<TAccountGame>
        : TAccountGame,
      TAccountPlayerChoice extends string
        ? WritableAccount<TAccountPlayerChoice>
        : TAccountPlayerChoice,
      ...TRemainingAccounts,
    ]
  >;

export type MakeChoiceInstructionData = {
  discriminator: ReadonlyUint8Array;
  gameId: bigint;
  choice: Choice;
};

export type MakeChoiceInstructionDataArgs = {
  gameId: number | bigint;
  choice: ChoiceArgs;
};

export function getMakeChoiceInstructionDataEncoder(): FixedSizeEncoder<MakeChoiceInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["gameId", getU64Encoder()],
      ["choice", getChoiceEncoder()],
    ]),
    (value) => ({ ...value, discriminator: MAKE_CHOICE_DISCRIMINATOR }),
  );
}

export function getMakeChoiceInstructionDataDecoder(): FixedSizeDecoder<MakeChoiceInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["gameId", getU64Decoder()],
    ["choice", getChoiceDecoder()],
  ]);
}

export function getMakeChoiceInstructionDataCodec(): FixedSizeCodec<
  MakeChoiceInstructionDataArgs,
  MakeChoiceInstructionData
> {
  return combineCodec(
    getMakeChoiceInstructionDataEncoder(),
    getMakeChoiceInstructionDataDecoder(),
  );
}

export type MakeChoiceInput<
  TAccountPlayer extends string = string,
  TAccountGame extends string = string,
  TAccountPlayerChoice extends string = string,
> = {
  player: TransactionSigner<TAccountPlayer>;
  game: Address<TAccountGame>;
  playerChoice: Address<TAccountPlayerChoice>;
  gameId: MakeChoiceInstructionDataArgs["gameId"];
  choice: MakeChoiceInstructionDataArgs["choice"];
};

export function getMakeChoiceInstruction<
  TAccountPlayer extends string,
  TAccountGame extends string,
  TAccountPlayerChoice extends string,
  TProgramAddress extends Address = typeof MAGICBLOCK_TUTORIAL_PROGRAM_ADDRESS,
>(
  input: MakeChoiceInput<TAccountPlayer, TAccountGame, TAccountPlayerChoice>,
  config?: { programAddress?: TProgramAddress },
): MakeChoiceInstruction<
  TProgramAddress,
  TAccountPlayer,
  TAccountGame,
  TAccountPlayerChoice
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? MAGICBLOCK_TUTORIAL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    player: { value: input.player ?? null, isWritable: true },
    game: { value: input.game ?? null, isWritable: true },
    playerChoice: { value: input.playerChoice ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("player", accounts.player),
      getAccountMeta("game", accounts.game),
      getAccountMeta("playerChoice", accounts.playerChoice),
    ],
    data: getMakeChoiceInstructionDataEncoder().encode(
      args as MakeChoiceInstructionDataArgs,
    ),
    programAddress,
  } as MakeChoiceInstruction<
    TProgramAddress,
    TAccountPlayer,
    TAccountGame,
    TAccountPlayerChoice
  >);
}

export type ParsedMakeChoiceInstruction<
  TProgram extends string = typeof MAGICBLOCK_TUTORIAL_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    player: TAccountMetas[0];
    game: TAccountMetas[1];
    playerChoice: TAccountMetas[2];
  };
  data: MakeChoiceInstructionData;
};

export function parseMakeChoiceInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedMakeChoiceInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      player: getNextAccount(),
      game: getNextAccount(),
      playerChoice: getNextAccount(),
    },
    data: getMakeChoiceInstructionDataDecoder().decode(instruction.data),
  };
}
